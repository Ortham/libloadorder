sudo: false
language: rust

# Dependencies of kcov, used by coverage
addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - binutils-dev
      - cmake
    sources:
      - kalakris-cmake

cache: cargo

install:
  - cargo install rustfmt || true
  - cargo install cargo-travis || true

script:
  - cargo fmt -- --write-mode=diff
  - cargo test
  - cargo coveralls --exclude-pattern=src/ffi/
